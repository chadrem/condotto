node_modules/
*.log
.env
.env.*
*.sqlite
*.sqlite-*
*.bak
# Compiled release binaries (`bun build --compile` via scripts/build-binary.ts).
dist/
# Bun leaves these intermediate artifacts in cwd during `--compile`.
*.bun-build
# The single config file holds secrets (Slack tokens) — track only the example.
conduit.toml
# Legacy config files (superseded by conduit.toml in M4 §1); ignored so any that
# still linger on an installed box are never committed.
conduit.repos.json
conduit.roles.json
